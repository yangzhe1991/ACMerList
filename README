php的爬虫，纯HTML的字符串处理。统计poj/hdoj刷题数和topcoder/codeforces的rating。写的比较烂，不过既然有需求还是发出来吧。
目前没有任何方法让他实时抓取瞬间完成，所以只能先统一抓取再显示。
sql是数据库导入，第二个表好像根本没用……因为当初想做一个若干时间之后未更新就自动更新，因为更新时间过慢无法让人等待因此放弃。
主代码的64行设1则访问php会更新所有人，更新完成后会正常输出，然后改成0，以后就直接从数据库读了。不过，如果想把这个放在学校ACM服务器，要考虑到教育网不能直接上外国网站的问题（不过acm.neu.edu.cn可以^_^）。最佳方式是在随便一台电脑运行程序，然后导出HTML文件直接上传到服务器。
rating的颜色是用jquery的，所以上传静态文件要把这个带上，随便一个jquery.js就可以。
newacmer就是一个方便新添加人员的表单，直接insert也可以。
